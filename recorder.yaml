
db_url: !secret recorder_db_url
purge_keep_days: 14
auto_purge: false
exclude:
  entities:
    - sun.sun
  entity_globs:
    - sensor.weather_*
    - sensor.sm_n960u_*
    - sensor.sm_n950u_*
  event_types:
    - call_service # Don't record service calls