
alias: Livingroom heat is on
trigger:
  platform: state
  entity_id: sensor.livingroom_heating
action:
  service_template: >
      {% set trigger={'to_state':{'state': 'heating'}} %}
      {% set option = trigger.to_state.state %}
      {{ '1' if option == 'heating' else '0' }}